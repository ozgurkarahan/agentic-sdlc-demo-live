{
  "intent": "Add rate limiting to the URL-shortener API so a single client can't exhaust the service.",
  "planApproved": true,
  "units": [
    {
      "id": "U1",
      "title": "limiter middleware",
      "paths": ["demos/sample-app/src/middleware/rateLimit.ts"],
      "parallelSafe": true,
      "dependsOn": []
    },
    {
      "id": "U2",
      "title": "config surface (RATE_LIMIT_MAX, RATE_LIMIT_WINDOW_MS)",
      "paths": ["demos/sample-app/src/config.ts"],
      "parallelSafe": true,
      "dependsOn": []
    },
    {
      "id": "U3",
      "title": "docs — rate-limit section",
      "paths": ["demos/sample-app/README.md"],
      "parallelSafe": true,
      "dependsOn": []
    },
    {
      "id": "U4",
      "title": "integration test (drives past the limit; asserts 429 + Retry-After)",
      "paths": ["demos/sample-app/test/e2e/rateLimit.e2e.test.ts"],
      "parallelSafe": false,
      "dependsOn": ["U1", "U2"]
    }
  ]
}
